dependencies {
    api(group: "mysql", name: "mysql-connector-java", version: mysqlConnectorVersion)
    api("org.testcontainers:junit-jupiter:${testContainerVersion}")
    api("org.testcontainers:mysql:${testContainerVersion}")
}

bootJar {
    enabled = false
}
